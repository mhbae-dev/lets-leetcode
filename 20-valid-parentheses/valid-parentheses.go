package leetcode

func isValid(s string) bool {
	var stack = []rune{}
	for i, char := range s {
		stack = append(stack, char)

	}
}

//loop over the string s
//check if the next item is the opposite closing bracket
// is not return false
//next item
